spring:
  application:
    name: Cart_Service

  datasource:
    url: jdbc:mysql://localhost:3306/cart_db_microservice
    username: root
    password: Dibya@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8084

spring:
  application:
    name: Product_Service

  datasource:
    url: jdbc:mysql://localhost:3306/product_db_microservice
    username: root
    password: Dibya@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8082

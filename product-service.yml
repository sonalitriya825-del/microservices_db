spring:
  application:
    name: PRODUCT-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/product_db
    username: root
    password: Dibya@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081
